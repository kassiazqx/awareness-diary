# Sync Readiness

阶段 1 不实现云同步，但数据模型必须避免未来无法同步。

## 必留能力

- 客户端生成稳定 ID，优先 UUID。
- `created_at`、`updated_at` 格式统一。
- 核心数据支持软删除或删除日志。
- 写入都走 service / repository，不能散在页面。
- 媒体文件存逻辑路径，不依赖不可迁移的本机绝对路径。
- 导入时需要冲突策略，默认可先用较新的 `updated_at` 获胜。

## 暂不决定

云服务选型、账号系统、实时同步策略可以第三阶段再定。
